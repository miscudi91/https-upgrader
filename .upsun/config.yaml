# Upsun configuration, generated using AI at: 2026-08-08T19:34:39.979Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  https-upgrader:
    type: composable:25.11
    stack:
      packages:
        - nginx
    build:
      flavor: none
    hooks:
      build: |
        set -ex
    web:
      commands:
        start: nginx -g 'daemon off;'
      locations:
        /:
          root: ''
          passthru: true
    mounts:
      conf:
        source: storage

routes:
  https://{default}/:
    type: upstream
    upstream: https-upgrader:http
